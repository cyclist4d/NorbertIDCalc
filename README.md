Norbert's Iron Dog Calculator
A self-contained single-page web app. Open `index.html` in any modern browser; no server, build process, or external dependencies are required.
The calculator reproduces the source Streamlit app's lookup behavior for Big Air, Extreme Vertical, and Speed Retrieve scores.
